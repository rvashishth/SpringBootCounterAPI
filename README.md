# Spring Boot Counter REST API

This project exposes an rest api which return the number of hit and timestamp format based on ISO 8601 standard

 * Build
 ```sh
 >gradlew build
  ```
 * Run
 ```sh
 >gradlew bootRun 
 ```
 * API
 	> GET http://localhost:8080/counterapi/counts
